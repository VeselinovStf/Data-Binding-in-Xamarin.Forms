<?xml version="1.0" encoding="utf-8" ?>
<Application xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="BethanysPieShopStockApp.App">
    <Application.Resources>
        <DataTemplate x:Key="PieOvervieListItemTemplate">
            <ViewCell>
                <Grid HeightRequest="70" Margin="5" BackgroundColor="White" RowSpacing="5" Padding="5">
                    <Grid.RowDefinitions>
                        <RowDefinition/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    <Image Source="{Binding ImageURI}" HorizontalOptions="Start"/>
                    <Label Grid.Column="1" Text="{Binding Name}"/>
                    <Label Grid.Column="2" Text="{Binding Price}" HorizontalOptions="End"/>
                </Grid>
            </ViewCell>
        </DataTemplate>
    </Application.Resources>
</Application>